[project]
name = "removing-duplicates"
version = "0.1.0"
description = "Pet Duplicate Scanner Suite with Tkinter GUI"
authors = [
    {name = "Aliaksandr",email = "yabarsukby@gmail.com"}
]
requires-python = ">=3.8,<3.15"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
